import SwiftUI

@main
struct MyApp: App {
  
    
    var body: some Scene {
        WindowGroup {
            // The very first SwiftUI view that appears at app launch
            MainTabView() // Show the tabs on launch
        }
    }
}
